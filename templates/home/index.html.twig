{% extends 'base.html.twig' %}

{% block title %}Hello HomeController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper text-center">
    <h1>Bienvenue sur notre platforme de Quiz </h1>
    {% if  not app.user %}
    <a href="{{path('app_register')}}" class="btn btn-secondary">S'inscrire</a>
    <a href= "{{path('app_login')}}" class="btn btn-secondary">S'identifier</a>
    {% endif %}
    {% if app.user %}
    <a href="{{path('app_game_create')}}" class="btn btn-secondary">Lancer une partie 🎮 </a>
    {% endif %}
</div>
{% endblock %}
